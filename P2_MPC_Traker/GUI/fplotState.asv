function fplotState(hAxis, state_array, idx)
    % Plot the state array on the specified axis
    switch idx
        case 2
            str = 
        case 3
        case 4
        case 5
        otherwise

    end
    line(hAxis, 1:length(state_array(:, idx)), state_array(:, idx), ...
         'DisplayName', strcat(" - State ", num2str(idx)));
    legend(hAxis, 'show');
end
